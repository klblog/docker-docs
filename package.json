{
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "release": "npm run docs:build && node build/move-file.js",
    "move-file": "node build/move-file.js"
  },
  "dependencies": {
    "vitepress": "1.0.0-rc.4"
  },
  "devDependencies": {
    "@types/node": "^20.4.10",
    "fs-extra": "^11.1.1"
  }
}